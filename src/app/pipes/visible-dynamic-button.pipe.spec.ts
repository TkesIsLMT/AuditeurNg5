import { VisibleDynamicButtonPipe } from './visible-dynamic-button.pipe';

describe('VisibleDynamicButtonPipe', () => {
  it('create an instance', () => {
    const pipe = new VisibleDynamicButtonPipe();
    expect(pipe).toBeTruthy();
  });
});
